spring.application.name=copalive_backend

# --- Connexion BDD ---
spring.datasource.url=jdbc:mysql://localhost:3306/foot_db?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sourya78@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# --- Scripts SQL (Data) ---
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:areas.sql,classpath:teams.sql,classpath:players.sql,classpath:matches.sql

spring.jpa.defer-datasource-initialization=true